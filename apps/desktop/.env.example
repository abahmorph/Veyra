# Copy to apps/desktop/.env to override the API endpoint baked into the
# renderer at build time (Vite: only VITE_* variables are exposed to the
# client). When unset the app falls back to http://localhost:8787.

# Veyra backend (server). Point this at the deployed server in production.
VITE_VEYRA_API=http://localhost:8787